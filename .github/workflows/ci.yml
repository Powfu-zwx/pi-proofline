name: CI

on:
  push:
    branches: [master]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - run: npm ci
      - run: npm run typecheck
      - run: npm test
      - run: npm run emit-vectors

      # Cross-implementation proof: bundles written by the TypeScript
      # extension must verify under the released Python reference.
      - run: pip install proofline
      - run: python tests/cross_verify.py
      - run: node examples/semantic-diff-demo.ts
